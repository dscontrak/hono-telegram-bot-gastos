# Cambiar nombre tabla `my_mensajes` a `my_tg_mensajes`

Cambiar la tabla de nombre sino se ha migrado aun cambiando la tabla `my_mensajes` a `my_tg_mensajes`, La carpeta de migracion esta en `src/db/migrations` y revisar todos 
archivos que se encuntrar en la carpeta `src/db`. 