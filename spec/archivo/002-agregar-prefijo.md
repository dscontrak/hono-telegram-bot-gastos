# Agregar un prefijo a la base de datos

Agegar un prefijo la la base datos esta aun no se han migrado aun.
La carpeta de migracion esta en `src/db/migrations` el prefijo seria `my_` y revisar todos 
archivos que se encuntrar en la carpeta `src/db`.